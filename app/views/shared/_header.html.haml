%header.flex-none.flex.flex-stretch.flex-center.border-bottom.bg-gray-darkerer.p2
  %h1.flex.flex-center.flex-auto.h2.m0= t('title.site')
  .flex-none.right-align
    - if admin_signed_in?
      = current_admin.email
      %br= link_to 'Logout', destroy_admin_session_path, :method => :delete
